extends Node
#velocidad "y" positiva en caer
var throw_velocity = Vector2(500,0)

var velocity = Vector2.ZERO	
var gravity = 0
var subiendo = false 
var Straccion = "nada"
func _process(delta):
	pass
